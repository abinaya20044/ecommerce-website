// Hero.js

import React from 'react';
import 'tailwindcss/tailwind.css';

const Hero = () => {
  return (
    <section id="hero">
      {/* Hero content */}
      {/* ... */}
      <button>Shop now</button>
    </section>
  );
};

export default Hero;
